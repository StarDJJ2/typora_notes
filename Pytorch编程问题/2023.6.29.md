# 1.对Dataframe使用len()

To obtain the length of a dataframe in Python, you can use the `len()` function on the dataframe itself. Here's an example:

```python
length = len(dataframe)
print("Length of the dataframe:", length)
```

This will give you the <font color='red'>number of rows</font> in the dataframe.